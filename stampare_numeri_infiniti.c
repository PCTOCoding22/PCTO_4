#include <stdio.h>

int main ()
{
    int x
    int y
    x=0
    y=0
    while (x<10&&y<10) 
    {
        while (x=0&&y<10)
        {
            y = y + 1
            printf ("%i", y);
        }
        x = x+1
        printf ("%i",x);
    }
    return 0
}