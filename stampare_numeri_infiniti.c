#include <stdio.h>

int main ()
{
    int x;
    int y
    x=0;
    y=x;
    while (x<10) 
    {
        y=0;
        while (y<10)
        {
            y = y + 1
            printf ("%i","%i\n" x, y);
        }
        x = x+1
    }
    return 0;
}