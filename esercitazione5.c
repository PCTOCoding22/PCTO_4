#include<stdio.h>

int main    ()
{
    int x;
    int y;
    printf("inserisci numero x");
    scanf("%i", &y);
    printf("inserisci numero y");
    scanf("%i", &y);
    if (x % y == 0)
    {
        printf("x non è multiplo di y");
    }
    else
    {
        printf("x non è multiplo di y");
    }
    return(0);    
}