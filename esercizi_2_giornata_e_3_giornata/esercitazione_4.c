#include <stdio.h>
int main()
{
  int x;
  int y;
  printf ("inseririsci l'età necessaria bper prendere la patente:");
   scanf("%i", &x);
   printf("inserisci la tua età");
  scanf("%i", &x);
  if(x >= y)
   {
       printf("puoi prendere la patente"); 
   }   
   else
   {
       printf("non puoi prendere la patente");
   }
   return (0);
  

}