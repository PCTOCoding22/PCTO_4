#include <stdio.h>

int main()
{
    int x;
    int y;
    printf("inserisci un numero x: ");
    scanf("%i", &x);
    printf("inserisci un numero y: ");
    scanf("%i", &y);
    if( x > y)
{
    printf("X: %i è maggiore di Y: %i\n", x, y);
}
else
{
printf("X: %i è maggiore di Y: %i\n", x, y);
}
return (0);
}